services:
  - type: web
    name: tokenomics-audit-ai
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run audit_app_v4.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: OPENAI_API_KEY
        sync: false
